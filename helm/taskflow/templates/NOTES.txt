TaskFlow has been deployed to namespace "{{ .Release.Namespace }}".

Workloads (plain Kubernetes Deployments, stateless in-memory backend):
  - taskflow-frontend  ({{ .Values.frontend.replicaCount }} replicas, HPA: {{ .Values.frontend.autoscaling.enabled }})
  - taskflow-backend   ({{ .Values.backend.replicaCount }} replicas, HPA: {{ .Values.backend.autoscaling.enabled }})

{{- if .Values.ingress.enabled }}
{{- if .Values.ingress.host }}

Access the app at:
  http://{{ .Values.ingress.host }}
{{- else }}

No ingress host is set (no owned domain for this environment). Find the
ALB's AWS-generated DNS name and hit that directly:
  kubectl get ingress -n {{ .Release.Namespace }} taskflow-frontend-ingress -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'
{{- end }}
{{- else }}

Ingress is disabled. Port-forward to reach the app locally:
  kubectl -n {{ .Release.Namespace }} port-forward svc/taskflow-frontend 8080:{{ .Values.frontend.service.port }}
{{- end }}

Watch a rollout in progress:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/taskflow-backend
  kubectl -n {{ .Release.Namespace }} rollout status deployment/taskflow-frontend
